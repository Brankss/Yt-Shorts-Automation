{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Brankss/Yt-Shorts-Automation/content/schemas/episode.schema.json",
  "title": "Episode",
  "description": "One published video: five dilemmas, their voiceover lines, and the metadata that ships with it. This file plus dna/video-dna.json is everything the render needs.",
  "type": "object",
  "required": [
    "episode",
    "blocks",
    "end_card",
    "publish"
  ],
  "additionalProperties": false,
  "properties": {
    "episode": {
      "type": "integer",
      "minimum": 1,
      "description": "Episode number. Appears in the badge and in the title — the numbered series is what makes viewers come back."
    },
    "slug": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$",
      "description": "Used in the output filename."
    },
    "edition": {
      "type": "string",
      "description": "Optional title tag, e.g. 'FOOD EDITION', 'HARD EDITION'. Measured to carry breakout videos at every channel size."
    },
    "blocks": {
      "type": "array",
      "minItems": 5,
      "maxItems": 5,
      "description": "Exactly five, in ramp order: easy+funny, easy, medium, hard, hardest. The DNA's 8500ms block length assumes five.",
      "items": {
        "type": "object",
        "required": [
          "dilemma_id",
          "stem",
          "option_a",
          "option_b",
          "reveal",
          "vo"
        ],
        "additionalProperties": false,
        "properties": {
          "dilemma_id": {
            "type": "string",
            "pattern": "^d[0-9]{4}$"
          },
          "stem": {
            "type": "string",
            "maxLength": 52,
            "description": "Spoken, never rendered. The screen shows only the two fragments."
          },
          "option_a": {
            "type": "string",
            "maxLength": 22
          },
          "option_b": {
            "type": "string",
            "maxLength": 22
          },
          "image_a": {
            "type": "string",
            "description": "Cut-out subject for the top field."
          },
          "image_b": {
            "type": "string",
            "description": "Cut-out subject for the bottom field."
          },
          "reveal": {
            "type": "object",
            "required": [
              "source"
            ],
            "additionalProperties": false,
            "description": "How this block ends. Either real poll numbers, or an explicit prediction with no number. There is no third option.",
            "properties": {
              "source": {
                "type": "string",
                "enum": [
                  "community_poll",
                  "comment_poll",
                  "authored",
                  "prediction_no_number"
                ],
                "description": "Where the numbers came from. community_poll needs the Community tab, which YouTube gates at 500 subscribers; comment_poll counts A/B replies on the previous episode; authored means written by us rather than measured — the channel owner's decision, and named explicitly so a file never quietly passes off an invented figure as a counted one; prediction_no_number carries no figure at all."
              },
              "a": {
                "type": "integer",
                "minimum": 0,
                "maximum": 100
              },
              "b": {
                "type": "integer",
                "minimum": 0,
                "maximum": 100
              },
              "votes": {
                "type": "integer",
                "minimum": 1
              },
              "prediction_favours": {
                "type": "string",
                "enum": [
                  "a",
                  "b"
                ]
              }
            },
            "allOf": [
              {
                "if": {
                  "properties": {
                    "source": {
                      "enum": [
                        "community_poll",
                        "comment_poll"
                      ]
                    }
                  }
                },
                "then": {
                  "required": [
                    "a",
                    "b",
                    "votes"
                  ]
                }
              },
              {
                "if": {
                  "properties": {
                    "source": {
                      "const": "authored"
                    }
                  }
                },
                "then": {
                  "required": [
                    "a",
                    "b"
                  ]
                }
              },
              {
                "if": {
                  "properties": {
                    "source": {
                      "const": "prediction_no_number"
                    }
                  }
                },
                "then": {
                  "required": [
                    "prediction_favours"
                  ],
                  "not": {
                    "anyOf": [
                      {
                        "required": [
                          "a"
                        ]
                      },
                      {
                        "required": [
                          "b"
                        ]
                      }
                    ]
                  }
                }
              }
            ]
          },
          "vo": {
            "type": "object",
            "required": [
              "setup"
            ],
            "additionalProperties": false,
            "description": "The spoken line. Only a setup: the reveal carries no voiceover, so the percentages land in silence and nothing explains them away.",
            "properties": {
              "setup": {
                "type": "string",
                "description": "Max 14 words. Poses the choice; never explains it."
              },
              "reaction": {
                "type": "string",
                "description": "Deprecated. The reveal is silent — a line here is ignored by the composition and by the timing."
              },
              "setup_duration": {
                "type": "number",
                "description": "Measured length of the rendered setup clip, in seconds, written back by make_voiceover.py. The block's timing is derived from it: the timer starts when the question stops, never before."
              }
            }
          }
        }
      }
    },
    "end_card": {
      "type": "object",
      "required": [
        "cta",
        "vo"
      ],
      "additionalProperties": false,
      "properties": {
        "cta": {
          "type": "string",
          "maxLength": 48,
          "description": "The word on screen. One word, large — 'COMMENT'."
        },
        "next_teaser": {
          "type": "string",
          "maxLength": 48
        },
        "vo": {
          "type": "string",
          "description": "Spoken over the end card while the tick runs underneath."
        },
        "vo_duration": {
          "type": "number",
          "description": "Measured length of the rendered end-card clip, written back by make_voiceover.py. The tick runs one second past it, then the chime closes the video."
        }
      }
    },
    "publish": {
      "type": "object",
      "required": [
        "title",
        "tags",
        "pinned_comment"
      ],
      "additionalProperties": false,
      "properties": {
        "title": {
          "type": "string",
          "maxLength": 100,
          "description": "Pattern: 'Would You Rather... #N' plus the edition when there is one. The numbered series is the measured winner in this niche."
        },
        "description": {
          "type": "string",
          "maxLength": 5000
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "maxItems": 15,
          "description": "From the measured tag cloud: would you rather, quiz, would you rather game, this or that, wyr, impossible choices."
        },
        "pinned_comment": {
          "type": "string",
          "maxLength": 200,
          "description": "Carries the poll link and the question that converts viewers into commenters."
        },
        "published_at": {
          "type": "string",
          "format": "date-time"
        },
        "video_id": {
          "type": "string",
          "description": "Filled after upload; used to pull retention in the measure stage."
        },
        "hashtags_in_title": {
          "type": "boolean",
          "description": "Measured: titles carrying real hashtags underperform in this niche. Kept as an explicit field so the decision is recorded per episode rather than forgotten."
        },
        "settings": {
          "type": "object",
          "additionalProperties": true,
          "description": "The YouTube upload settings this episode ships with, so a re-upload or a second channel reproduces them exactly instead of relying on whatever the studio remembered.",
          "properties": {
            "made_for_kids": {
              "type": "boolean",
              "description": "Must be false. Made-for-kids disables comments, and comments are this format's engine."
            },
            "age_restricted": {
              "type": "boolean"
            },
            "category": {
              "type": "string"
            },
            "language": {
              "type": "string"
            },
            "license": {
              "type": "string"
            },
            "comments": {
              "type": "string"
            },
            "allow_remixing": {
              "type": "boolean"
            },
            "paid_promotion": {
              "type": "boolean"
            },
            "playlist": {
              "type": "string"
            },
            "visibility": {
              "type": "string"
            }
          }
        }
      }
    },
    "dna_version": {
      "type": "string",
      "description": "The video-dna.json version this episode was rendered against, so a style change can be correlated with a retention change."
    }
  }
}